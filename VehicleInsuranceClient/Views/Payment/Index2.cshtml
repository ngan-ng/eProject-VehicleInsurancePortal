@*@model VehicleInsuranceClient.Models.CertificateModel

@{
    ViewData["Title"] = "Index2";
}

<h1>Index2</h1>

<div>
    <h4>CertificateModel</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PolicyNo)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PolicyNo)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Premium)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Premium)
        </dd>
        @using (Html.BeginForm("PayLater", "Payment", FormMethod.Post, new { @class = "m-2" }))
        {
            @Html.AntiForgeryToken()
            <input asp-for="@Model.Amount" value="@Model.Premium" type="hidden" />
            <input asp-for="@Model.PolicyNo" value="@Model.PolicyNo" type="hidden" />
            <button type="submit" class="btn btn-secondary btn_estimate rounded-1 pt-1 mb-3 w-100">
                <strong class="">Pay Later</strong>
            </button>
        }
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
*@